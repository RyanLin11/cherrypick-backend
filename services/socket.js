const { io } = require('./sockets');
const Election = require('../schemas/election');
const Vote = require('../schemas/vote');

class SocketUtils {
    static async join(electionId) {

    }
    static async start(electionId) {

    }
    static async update(electionId) {
        
    }
}

module.exports = SocketUtils;